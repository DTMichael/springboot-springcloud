server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server  #eureka实例得主机名
  client:
    register-with-eureka: false  #不把eureka-server本生注册进注册中心
    fetch-registry: false  #不从eureka上获取服务得注册信息
    service-url:
      defaultZone: http://localhost:8761/eureka/  #注册中心服务注册地址